{
  "mcpServers": {
    "firebase": {
      "command": "npx",
      "args": [
        "-y",
        "firebase-tools@latest",
        "mcp",
        "--dir",
        "C:/RFT/Projects/CRM",
        "--only",
        "auth,firestore,storage"
      ]
    }
  }
}
